// SPDX-License-Identifier: Apache-2.0
//
// dashboard.js — the Activity view (charts). Stub (classic script →
// window.Chippy.dashboard). Future: the five time-range comment-inflow pies,
// task-state and goal-state pies, the monthly timeline, and the cumulative +
// open-task-balance charts — hand-rolled SVG, no external library. Step 13.

(function (root) {
  const Chippy = (root.Chippy = root.Chippy || {});
  Chippy.dashboard = {};
})(typeof globalThis !== 'undefined' ? globalThis : this);
