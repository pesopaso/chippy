// SPDX-License-Identifier: Apache-2.0
//
// ui.js — reusable, data-agnostic controls. Stub.
// Future: the single safeSetHtml/DOMPurify boundary, renderEntryText (markdown +
// inline images + @[Name] chips + URL links), tag-chip input, #/@ autocomplete,
// state dropdown, priority dot, due-date picker, action modal, toast, image
// overlay/gallery. Implemented from Step 5 onward.

export {};
